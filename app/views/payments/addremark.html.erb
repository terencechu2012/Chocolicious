<div class='row'>
	<div class='col-sm-12'>
		<div class='page-header'>
			<h1 class='pull-left'><i class='icon-edit'></i><span>Reject Claim</span></h1>
		</div>
	</div>
</div>

<div class='row'>
	<div class='col-sm-12 col-lg-12'>
		<div class='box bordered-box'>
			<div class='box-content box-double-padding'>
				<%= form_for @rejectpayment, :url => { :action => "confirmrejectpayment" }, :method => :POST  do |f|  %>
				<fieldset>
					<%= f.hidden_field(:id, :value => @rejectpayment.id)%>
					<div>
						<b>You are rejecting claim submmited by <u><%= @rejectpayment.userid %></u> from <u><%= @rejectpayment.clubid %></u> with amount <u><%= @rejectpayment.amount %></u>.</b>
					</div>
					<div>
						Please add remark for the rejection:
					</div>
					<div class='form-group'>
						<%= f.hidden_field(:status, :value => @status)%>
						<%= f.text_area :remarks, size: "100 x 5", :required => true, :maxlength => 255 %>
					</div>
				</fieldset>
				<div class='form-actions form-actions-padding' style='margin-bottom: 0;'>
					<div class='text-right'>
						<%= f.submit("Confirm", :class=>"btn btn-primary btn-lg red-background")%>
					</div>
				</div>
				<% end %>
			</div>
		</div>
	</div>
</div>
