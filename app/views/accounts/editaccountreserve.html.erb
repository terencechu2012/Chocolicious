<div class='row'>
	<div class='col-sm-12'>
		<div class='page-header'>
			<h1 class='pull-left'><i class='icon-edit'></i>
				<!-- span has to be on next line to create that space between the logo and the words -->
				<span>Edit Reserve Account</span></h1>
		</div>
	</div>
</div>
<!-- Any permissions required: insert here -->
<h1>Edit Balances in <%=@club.clubid%></h1>

<%=form_for @club, :url => { :action => "updatereserve" } do |f|%>
<table>
	<tr>
		<th align="right">Balance: </th><td><%= f.number_field(:balance, :step => 0.01) %></td>
	</tr>
	<tr>
		<th align="right">Limit: </th><td><%= f.number_field(:limit, :step => 0.01) %></td>
	</tr>
	
</table>
<%= f.hidden_field(:clubid, :value=>@club.clubid)%>
<br />
<%= submit_tag("Update Record", :class=>"btn btn-primary btn-lg") %></th>
<%end%>
